/* exported last */
function last(array) {
  return array[array.length - 1];
}

//  define a function, last, which take a single parameter, "array"
//  return the value of the last index of the "array"
