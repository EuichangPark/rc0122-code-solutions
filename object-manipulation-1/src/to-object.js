/* exported toObject */
function toObject(keyValuePair) {
  var object = {};
  object[keyValuePair[0]] = keyValuePair[1];
  return object;
}

//  define a function named toObject with a single parameter "keyValuePari"
//  define a variable with a bracket literal
//  Use bracket notation to add key and value to the object
//  return the object
