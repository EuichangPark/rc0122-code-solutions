/* exported lastChars */
function lastChars(length, string) {
  var result;
  result = string.substring(string.length - length, string.length);
  return result;
}

//  define a function named lastChars with two parameters "length" and "string"
//  assign variable named "result" with nothing
//  re assign the result to string that is from last length of characters
//  return the result
