/* exported firstChars */
function firstChars(length, string) {
  var result;
  result = string.substring(0, length);
  return result;
}

//  define a function named firstChars with two parameters "length" and "string"
//  assign a variable named "result" with nothing
//  result is going to be the string that's from first character to the "length"
//  return th result
