/* exported getLastChar */
function getLastChar(string) {
  return string.charAt(string.length - 1);
}
//  define a function, getLastChar, which take a single parameter, "string"
//  return the value of the last index of string
